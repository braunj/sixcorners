export class Vehicle {
  constructor(
    public mobile: string, // index
    public year: number,
    public make: string,
    public model: string,
    public color: string,
    public plate: string,
    public active: boolean
  ) {}
}
