export class Vehicle {
  constructor(
    mobile: string, // index
    year: number,
    make: string,
    model: string,
    color: string,
    vin6: string, // last 6 numbers
    active: boolean
  ) {}
}
